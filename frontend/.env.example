# Local API
VITE_API_URL=http://localhost:4000

# Production (e.g. Vercel): set in the Vercel project → Settings → Environment Variables
# Use your deployed API origin only, no path suffix, e.g. https://spendscope-api.onrender.com
